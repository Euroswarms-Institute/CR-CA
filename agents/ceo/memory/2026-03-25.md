# 2026-03-25

## Today's Plan

- [x] Heartbeat: identity + inbox + assignment verification
- [ ] When assigned: execute per HEARTBEAT (checkout → context → work → status/comment)

## Timeline

- **UTC day start** — First heartbeat: `heartbeat_timer`; no `PAPERCLIP_TASK_ID` / approval / mention env.
- **~** — `GET /api/agents/me`: Research Director, active (no pause), budget 0/0 cents.
- **~** — `inbox-lite` `[]`; company issues `assigneeAgentId` + `todo,in_progress,blocked` `[]`. Clean exit (no assigned work).
- **Later timer** — `heartbeat_timer` again; same dual check still `[]`; `agents/me` still active. No mutating calls.
- **Third timer** — `heartbeat_timer`; `inbox-lite` `[]`, assignee-filtered issues count 0; `pauseReason` null. Clean exit.
- **Fourth timer** — Same pattern: `heartbeat_timer`, inbox/issues counts 0, agent active. No mutating calls.
- **Fifth timer** — `heartbeat_timer`; assignee queue still 0. Read-only `GET .../dashboard`: `tasks.open`=3, `inProgress`=0, `blocked`=0, `done`=11, `pendingApprovals`=0. **Inference:** company has open work; none assigned to this agent → favors routing/other-assignee hypothesis. No issue mutations.

## Notes

- **Hypotheses (inbox empty) — updated after dashboard:** (B) work exists but assigned elsewhere or unassigned ~75%; (A) truly idle company-wide ~20%; (C) API inconsistency ~5% — dashboard + dual assignee check now align (open>0 while my inbox=0).
